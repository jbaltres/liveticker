import React from "react";
import LocationWeekContent from "../components/LocationWeekContent";

export default {
  title: "LocationWeekContent"
};

export const DefaultWeekContent = () => <LocationWeekContent />;
